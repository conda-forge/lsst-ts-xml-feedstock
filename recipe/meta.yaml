{% set name = "lsst-ts-xml" %}
{% set version = "28.0.1" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/lsst-ts/ts_xml/archive/v{{ version }}.tar.gz
  sha256: 50fbee971fc957d7c70de9e2926b341f54a1f822dcf4e017cdf4a83ee11d758c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
    - setuptools_scm
  run:
    - python >={{ python_min }}
    - astropy-base
    - fastavro
    - __unix

test:
  source_files:
    - tests
  imports:
    - lsst.ts.xml
  requires:
    - python {{ python_min }}
    - pytest
    - lxml
  commands:
    - pytest

about:
  home: https://github.com/lsst-ts/ts_xml
  summary: 'Contains XML files used to generate SAL data.'
  description: |
    ts-xml is used by ts_sal to generate the commands, events and telemetry
    for various components used by the Vera C. Rubin Observatory for the
    Simonyi Survey Telescope.
  license: GPL-3.0-only
  license_family: GPL
  license_file: LICENSE
  doc_url: https://ts-xml.lsst.io/
  dev_url: https://github.com/lsst-ts/ts_xml

extra:
  recipe-maintainers:
    - couger01
    - tribeiro
    - rbovill
